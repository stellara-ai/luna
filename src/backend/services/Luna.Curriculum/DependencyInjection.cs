namespace Luna.Curriculum;

using Microsoft.Extensions.DependencyInjection;

/// <summary>
/// Dependency injection configuration for Luna.Curriculum module.
/// Lessons, subjects, scope & sequence, teaching strategies.
/// </summary>
public static class DependencyInjection
{
    public static IServiceCollection AddCurriculumModule(this IServiceCollection services)
    {
        // Lesson repository
        // Subject definitions
        // Teaching strategy selector

        return services;
    }
}
